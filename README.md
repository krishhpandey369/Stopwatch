# Stopwatch
The project is a small Python application that works as a basic stopwatch. It was created using Tkinter, which is Python’s standard GUI module. The app can start the timer, stop it, and reset it back to zero.
